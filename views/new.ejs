<% include ./partials/header %>
<div class="ui main text container segment">
    <div class="ui huge header">New Entry</div>
    <form class="ui form" action="/blogs" method="POST">
        <div class="field">
            <label>Artist</label>
            <input type="text" name="blog[artist]" placeholder="artist">
        </div>
        <div class="field">
            <label>Release Title</label>
            <input type="text" name="blog[release]" placeholder="release title">
        </div>
        <div class="field">
            <label>Image</label>
            <input type="text" name="blog[image]" placeholder="image">
        </div>
        <div class="field">
            <label>Track Listing</label>
            <textarea name="blog[body]" placeholder="track listing"></textarea>
        </div>
            <input class="ui violet big button" type="submit">
    </form>
</div>

<% include ./partials/footer %>